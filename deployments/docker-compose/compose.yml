services:
  server:
    image: ghcr.io/lyonbrown4d/orivis-server:${ORIVIS_IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file:
      - server.env
    ports:
      - "${ORIVIS_HTTP_PORT:-8080}:8080"
    volumes:
      - orivis-data:/data
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 10s
      timeout: 3s
      retries: 12

  agent:
    image: ghcr.io/lyonbrown4d/orivis-agent:${ORIVIS_IMAGE_TAG:-latest}
    restart: unless-stopped
    user: root
    env_file:
      - agent.env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    depends_on:
      server:
        condition: service_healthy
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 12
    labels:
      orivis.enable: "true"
      orivis.environment: "dev"
      orivis.monitor.redis.type: "redis"
      orivis.monitor.redis.target: "redis://redis:6379"
      orivis.monitor.redis.name: "compose-redis"
      orivis.monitor.redis.enabled: "true"
      orivis.monitor.redis.interval: "10s"
      orivis.monitor.redis.timeout: "3s"
      orivis.monitor.redis.retry: "0"
      orivis.monitor.redis.aggregation: "majority_down"

  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: orivis
      POSTGRES_PASSWORD: orivis
      POSTGRES_DB: orivis
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U orivis -d orivis"]
      interval: 10s
      timeout: 3s
      retries: 12
    labels:
      orivis.enable: "true"
      orivis.environment: "dev"
      orivis.monitor.postgres.type: "postgres"
      orivis.monitor.postgres.target: "postgres://orivis:orivis@postgres:5432/orivis?sslmode=disable"
      orivis.monitor.postgres.name: "compose-postgres"
      orivis.monitor.postgres.enabled: "true"
      orivis.monitor.postgres.interval: "10s"
      orivis.monitor.postgres.timeout: "3s"
      orivis.monitor.postgres.retry: "0"
      orivis.monitor.postgres.aggregation: "majority_down"

volumes:
  orivis-data:
